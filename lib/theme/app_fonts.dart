/// Familles de polices embarquées dans l'application.
///
/// Les fichiers sont déclarés dans la section `fonts` de `pubspec.yaml` : ils
/// voyagent avec l'app plutôt que d'être téléchargés au premier lancement, ce
/// qui garantit le bon rendu hors ligne.
class AppFonts {
  static const sans = 'InstrumentSans';
  static const serif = 'Spectral';
  static const mono = 'JetBrainsMono';
}
